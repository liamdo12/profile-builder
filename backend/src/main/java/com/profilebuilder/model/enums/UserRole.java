package com.profilebuilder.model.enums;

/**
 * User roles for authorization.
 * BASIC — free tier, PREMIUM — paid tier, ADMIN — full access.
 */
public enum UserRole {
    BASIC,
    PREMIUM,
    ADMIN
}
